//Mobina Ahmadimasoud, st067173@student.spbu.ru, 24.B83
// Header guard - prevents multiple inclusions of this file
#ifndef HEADER_H
#define HEADER_H

// Standard exception handling (for runtime_error, invalid_argument, etc.)
#include <stdexcept>

// Standard input/output streams (cout, cin, cerr)
#include <iostream>

// String class and operations
#include <string>

// String stream operations (istringstream, ostringstream)
#include <sstream>

// C standard library utilities (exit(), atoi(), rand(), etc.)
#include <cstdlib>

// End of header guard
#endif
