//Mobina Ahmadimasoud, st067173@student.spbu.ru, 24.B83
// Prevent multiple inclusions with header guards
#ifndef HEADER_H
#define HEADER_H

// Standard input/output streams (cin, cout, cerr, clog)
#include <iostream>

// File stream operations (ifstream, ofstream, fstream)
// Required for reading/writing files
#include <fstream>

// Dynamic array container (vector) and related operations
// Provides resizable array functionality
#include <vector>

// String class and string manipulation functions
// Enables modern string handling
#include <string>

// Filesystem operations
// Allows directory navigation, file info, path manipulation
#include <filesystem>

// Input/output formatting (setw, setprecision, etc.)
// Controls text alignment, numeric formatting
#include <iomanip>

// Algorithm utilities (sort, find, transform, etc.)
// Common data processing functions
#include <algorithm>
#endif
